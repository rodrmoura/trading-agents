# LLM Gateway Python SDK

Placeholder for the Python SDK that calls the VS Code LLM gateway.

Expected future responsibilities:

- provide a small Python client for gateway endpoints
- expose a LangChain-compatible adapter
- support structured output and tool-calling compatibility where possible
- provide clear exceptions for gateway/model/auth failures

This package should remain generic and avoid finance assumptions.
