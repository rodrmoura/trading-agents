# Agent Runtime

Placeholder for the generalized agent collaboration runtime.

Expected future responsibilities:

- load agent manifests and workflow graphs
- route model calls through gateway-compatible model adapters
- coordinate tools, shared state, artifacts, memory, checkpoints, and events
- support human approval points
- emit replayable event logs for a future frontend

This package should not import TradingAgents.
