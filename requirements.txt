# Runtime dependencies are declared in pyproject.toml [project.dependencies].
# This file is a compatibility/test-install shim for tools and users that
# expect requirements.txt; keep it aligned when dependency or test-runner
# requirements change.
.
pytest>=8.3
